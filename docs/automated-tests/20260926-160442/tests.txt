  PASS  harness: the runner is on the shared kit and reports its revision
  PASS  harness: the addon's load list is DERIVED from the TOC, in TOC order (testing-§9)
  PASS  harness: every derived addon path exists on disk
  PASS  harness: no libs/ path leaked into the derived addon list
  PASS  harness: the explicit LibKa0s list matches LibKa0s.xml, in XML order (anti-patterns #48)
  PASS  harness: every LibKa0s file the runner loads exists on disk
  PASS  harness: the libraries load BEFORE the addon's own files
  PASS  harness: the addon is a named AceAddon the kit can look up
  PASS  harness: the addon's event registrations reach the kit's dispatcher
  PASS  harness: UnregisterAllEvents silences what the dispatcher reaches
  PASS  harness: a registration naming a method the addon lacks is refused
  PASS  harness: the addon's AceTimer handles are the kit's, on the kit's queue
  PASS  events: one retired event name does not abort OnEnable
  PASS  events: one retired event name does not abort OnEnable on a client without C_EventUtils
  PASS  events: the [Init] summary carries no rejected clause when every name registered
  PASS  degraded: the Core stub's SafeRegisterEvent survives a bad name
  PASS  events: a stand-up after a rejection records the name once
  PASS  libka0s: every vendored major registers under LibStub
  PASS  libka0s: MODULES names every file of every major, at a positive integer minor
  PASS  core: the published seams ARE the library's, not a lookalike
  PASS  core: the close button is the library's, told which addon folder is asking
  PASS  core: ApplySkin stays a bare bind, so its optional override survives
  PASS  core: the printer emits <prefix><space><body> as one line
  PASS  core: the prefix is read at CALL time, not captured at load
  PASS  core: the sink is the Lua global print, so the harness can see chat output
  PASS  debuglog: the console is the library's instance, and the sink is bound bare
  PASS  debuglog: the library is told the FOLDER name, not just the frame name
  PASS  debuglog: the descriptor keeps the frame globals the old console used
  PASS  debuglog: the composed window title is unchanged
  PASS  debuglog: the flag stays the addon's — the library never keeps a copy
  PASS  debuglog: the [Init] summary is the addon's, reached through the descriptor
  PASS  debuglog: the console's user-visible strings resolve to prose, not to their own keys
  PASS  debuglog: the gated sink survives a format its arguments cannot satisfy (WG-22)
  PASS  options: Settings.Helpers IS the library instance, decorated in place
  PASS  options: the host's data seams survived the move onto the instance
  PASS  options: the host's RestoreAllDefaults deliberately overrides the library's
  PASS  options: a panel write takes the addon's single write seam
  PASS  options: no layout constant is restated in this addon's own source
  PASS  options: the panel body still builds on the NEXT frame, not inside OnShow
  PASS  slash: the help header is the library's, with this addon's alias sentence
  PASS  slash: a help row is the one command-row formatter, indented two spaces
  PASS  slash: the landing page renders the SAME rows, un-indented (convergence #2)
  PASS  slash: the landing page draws those rows and nothing of its own
  PASS  slash: `list` renders through the shared key/value formatter
  PASS  slash: a number row still renders through its schema `fmt`
  PASS  slash: `toggle` survived the adoption, through the descriptor's parse adapter
  PASS  slash: the descriptor's L overrides exactly one string and nothing else
  PASS  slash: every user-visible CLI string resolves to prose, not to its own key
  PASS  degraded: the addon loads with LibKa0s absent
  PASS  degraded: the cause clause is published on BOTH paths
  PASS  degraded: the printer announces the absence exactly ONCE, then prints normally
  PASS  degraded: the fallback printer still degrades a secret in place
  PASS  degraded: every DebugLog member the addon calls still answers
  PASS  degraded: the console stub copies NO library formatter
  PASS  degraded: every HAND-WRITTEN schema row survives the options library's absence (options-ui-§1)
  PASS  degraded: the STORED profile is the same shape with the library absent
  PASS  degraded: `/wg disable` and `/wg enable` print the library-absent line and write nothing (options-ui-§1, WhatGroup#22)
  PASS  degraded: `/wg test on|off` print the library-absent line and move nothing
  PASS  degraded: Reset all settings still resets the profile (options-ui-§1)
  PASS  degraded: the settings stub carries no widget maker and no layout constant
  PASS  degraded: the settings panel explains itself once at load and once per config
  PASS  degraded: a bare /wg runs `config`, as the library's dispatcher does
  PASS  degraded: `/wg debug on` still moves the flag and explains the missing window ONCE
  PASS  libka0s: no seam re-spells the refusal line (slash-commands-§7)
  PASS  degraded: the Slash stub's DisabledLine uses the library's DISABLED_LINE_FORMAT bytes
  PASS  libka0s: the Master controls hook is keyed off the library's constant, not a copy of it
  PASS  libka0s: the L-trap matcher flags the table and the `or` spelling, not the `and` one
  PASS  libka0s: no seam file hands a descriptor this addon's locale table (the L trap)
  PASS  libka0s: Core has no STRINGS and reads no descriptor L (tripwire)
  PASS  libka0s: Options reads no descriptor L (tripwire)
  PASS  locale: every key enUS.lua defines has a reader
  PASS  parity: the Core seam's whole namespace surface survives the library's absence
  PASS  parity: the DebugLog stub carries the whole live surface
  PASS  parity: the Slash stub carries the whole live surface
  PASS  parity: the Options helpers stub carries the whole live surface
  PASS  parity: the Schema host stub's instance carries the whole live instance surface
  PASS  parity: the Schema host stub carries the library's own members
  PASS  parity: the Launcher stub carries the whole live surface
  PASS  parity: the Lifecycle stub carries the whole live surface
  PASS  parity: the Compat reader arm carries every library member the addon wires
  PASS  mediasetup: NS.Icon answers the vendored path, EXTENSIONLESS
  PASS  mediasetup: an icon the library does not ship answers nil
  PASS  mediasetup: NS.MediaFont answers the vendored face, and only for a face it ships
  PASS  mediasetup: the face this addon names is the face the library registers
  PASS  mediasetup: the LSM registration is the library's, made at file load
  PASS  mediasetup: every icon this addon draws is one the library ships
  PASS  mediasetup: every icon this addon draws has a file in the vendored copy
  PASS  mediasetup: the whole catalog has a file in the vendored copy
  PASS  mediasetup: this addon ships no private copy of the shared art (anti-patterns #63)
  PASS  mediasetup: with no library there is no art and no face, and that is not an error
  PASS  mediasetup: a degraded install still gets a REAL font, never nil and never a dead path
  PASS  envsetup: NS.Meta reads this addon's TOC
  PASS  envsetup: NS.Meta asks about this addon's FOLDER, not its title or its frame prefix
  PASS  envsetup: NS.Meta degrades to nil when the client exposes no manifest reader
  PASS  envsetup: NS.Version prefers the TOC over this addon's own constant
  PASS  envsetup: NS.Version falls back to this addon's own constant
  PASS  envsetup degraded: an install with no LibKa0s still reads its own TOC
  PASS  envsetup: no file inlines its own C_AddOns ladder any more
  PASS  envsetup: the ladder did not land in Compat either
  PASS  util: SafeToString handles nil / booleans / strings / numbers
  PASS  util: SafeToString yields <secret> for a value that raises in concat
  PASS  util: IsConcatSafe true for scalars, false for a raising value
  PASS  util: NS.Print prepends the [WG] prefix and stringifies each arg
  PASS  util: NS.Print degrades a secret-like arg in place, never raising
  PASS  util: Windows.Save/Restore round-trips a frame point through db.global (WG-26)
  PASS  util: Windows.Restore is a no-op when nothing is saved (WG-26)
  PASS  util: SafeToString renders a negative and a fractional number
  PASS  util: SafeToString degrades a function and a coroutine
  PASS  util: IsConcatSafe reports a boolean as unsafe
  PASS  util: IsConcatSafe reports nil as safe (concat of an empty table)
  PASS  util: NS.Print with no arguments still prints the prefix
  PASS  util: NS.Print joins several arguments with single spaces
  PASS  util: NS.Print stringifies nil and boolean arguments in place
  PASS  util: PointOf reads a frame's primary anchor
  PASS  util: PointOf defaults relPoint to the point and offsets to zero
  PASS  util: PointOf returns nil for a frame with no anchor yet
  PASS  util: PointOf returns nil for nil or a non-frame
  PASS  util: Save is a no-op before the db is ready
  PASS  util: Save skips a frame that has no anchor
  PASS  util: Save overwrites a previously stored point
  PASS  util: windows are stored under independent names
  PASS  util: Restore returns false for a frame that cannot be anchored
  PASS  util: Restore is a no-op before the db is ready
  PASS  util: Restore clears existing anchors before applying the saved one
  PASS  util: a Save/Restore round trip survives through the real frame stub
  PASS  util: FormatDuration renders hours, minutes and seconds
  PASS  util: FormatDuration drops units above the largest non-zero one
  PASS  util: FormatDuration keeps trailing zero units once a larger one is present
  PASS  util: FormatDuration rounds fractional seconds up
  PASS  util: FormatDuration renders a non-positive duration as 0s
  PASS  compat: GetSpellName returns the C_Spell name
  PASS  compat: GetSpellTexture is non-nil (caller supplies default)
  PASS  compat: GetSpellLink returns a hyperlink for the spell
  PASS  compat: IsSpellKnown true when learned
  PASS  compat: the harness answers a learned spell through C_SpellBook, not the global
  PASS  compat: IsSpellKnown false when not learned
  PASS  compat: GetSpellCooldownRemaining is 0 for a spell that is ready
  PASS  compat: GetSpellCooldownRemaining counts down from start + duration
  PASS  compat: GetSpellCooldownRemaining is 0 once the cooldown has elapsed
  PASS  compat: GetSpellCooldownRemaining ignores a global-cooldown-length window
  PASS  compat: GetSpellCooldownRemaining reports 0 when the cooldown is disabled
  PASS  compat: GetSpellCooldownRemaining returns 0 when the API is missing
  PASS  compat: GetActivityInfoTable passes the table through
  PASS  compat: GetSpellName falls back to the legacy GetSpellInfo global
  PASS  compat: GetSpellName falls through when the modern API returns nil
  PASS  compat: GetSpellName returns nil when no API exists at all
  PASS  compat: GetSpellTexture falls back to the legacy global
  PASS  compat: GetSpellTexture returns nil with no API (the caller supplies a default)
  PASS  compat: GetSpellLink returns nil with no API (the caller renders plain text)
  PASS  compat: IsSpellKnown normalizes to a plain boolean
  PASS  compat: IsSpellKnown returns false when the API is missing
  PASS  compat: IsSpellKnown asks C_SpellBook first when both APIs exist
  PASS  compat: IsSpellKnown takes a false from C_SpellBook as the answer
  PASS  compat: IsSpellKnown uses the global when C_SpellBook or its member is absent
  PASS  compat: IsSpellKnown returns false when neither API exists
  PASS  compat: GetActivityInfoTable returns nil for an unknown activity
  PASS  compat: GetActivityInfoTable returns nil when C_LFGList is absent
  PASS  compat: AddOnLinkType answers Blizzard's addon link type
  PASS  compat: AddOnLinkType is nil without LinkTypes.AddOn
  PASS  compat: AddOnLinkType is nil without EventRegistry:RegisterCallback
  PASS  compat: Compat is the sole namespace the addon reads variant APIs through
  PASS  compat: GetSpellCooldownTimes hands SetCooldown exactly two values on every rung
  PASS  compat: GetSpellCooldownRemaining reads a legacy isEnabled of 0 as disabled, 1 and nil as enabled
  PASS  compat: GetSpellCooldownRemaining reads a modern table with no isEnabled as enabled
  PASS  compat: GetSpellCooldownRemaining is 0 for a cooldown table with no start or duration
  PASS  compat: GetSpellName and GetSpellTexture answer the popup's numeric teleport ids
  PASS  compat: GetSpellName and GetSpellTexture ARE LibKa0s-Compat-1.0's members
  PASS  compat: GetSpellName falls through a plain empty string from the modern rung
  PASS  compat: GetSpellName reads C_Spell.GetSpellInfo's name when GetSpellName has none
  PASS  compat: an id outside the spell domain answers the no-answer value without asking the client
  PASS  compat: GetSpellTexture hands back one value when the client answers two
  PASS  compat degraded: with LibKa0s absent the spell readers answer the library's absent values
  PASS  database: fresh DB lands at schemaVersion 1
  PASS  database: RunMigrations is idempotent
  PASS  database: RunMigrations re-seeds a missing schemaVersion
  PASS  database: defaults declare global.schemaVersion 0 (savedvariables-§1)
  PASS  database: the stamp survives AceDB's logout strip, so the first real migration runs
  PASS  database: a raising step leaves the stamp at the last completed version
  PASS  database: RunMigrations before the db exists is a no-op
  PASS  database: an older saved DB is stepped up to the current version
  PASS  database: a version move is logged, a no-op migration is silent (debug-logging-§8)
  PASS  database: migrations run before any profile read (OnInitialize order)
  PASS  database: the profile is untouched by a migration pass
  PASS  settings: BuildDefaults threads profile + global defaults
  PASS  settings: defaults source from NS.C (defaults/Profile.lua, WG-24)
  PASS  settings: BuildDefaults seeds an empty global.windows table (WG-26)
  PASS  settings: debug is not a persisted schema row (WG-12)
  PASS  settings: ValidateSchema reports zero errors
  PASS  settings: Get/Set round-trips through db.profile
  PASS  settings: RestoreAllDefaults resets a changed value
  PASS  settings: RestoreAllDefaults prunes orphaned profile keys (F1)
  PASS  settings: RestoreAllDefaults deep-copies table defaults (F2)
  PASS  settings: RestoreAllDefaults skips per-row onChange (F3)
  PASS  settings: enabled=false onChange wipes capture
  PASS  settings: every schema row declares the fields the panel and CLI need
  PASS  settings: schema paths are unique
  PASS  settings: every schema row carries a tooltip
  PASS  settings: every number row declares min, max and step
  PASS  settings: ValidateSchema counts each defect on a broken row
  PASS  settings: ValidateSchema reports a non-table row
  PASS  settings: a broken row does not stop the panel registering
  PASS  settings: BuildDefaults nests dotted paths into real subtables
  PASS  settings: BuildDefaults covers every schema row
  PASS  settings: BuildDefaults deep-copies table defaults
  PASS  settings: BuildDefaults is a fresh table each call
  PASS  settings: Get returns nil before the db exists
  PASS  settings: Set before the db exists is a harmless no-op
  PASS  settings: a write creates the intermediate tables it walks through
  PASS  settings: Get on an unknown deep path returns nil and creates no table
  PASS  settings: Resolve replaces a non-table intermediate
  PASS  settings: there is no RawSet; a write to `enabled` always runs its onChange
  PASS  settings: Set takes no skipOnChange option
  PASS  settings: a throwing onChange propagates, after the value landed
  PASS  settings: Set on a path with no schema row is refused and stores nothing
  PASS  settings: FindSchema matches on the exact path
  PASS  settings: RestoreAllDefaults restores every schema row
  PASS  settings: RestoreAllDefaults leaves db.global untouched
  PASS  settings: RefreshAll runs every refresher on the open page, in registration order
  PASS  settings: a throwing refresher does not abort the sweep
  PASS  settings: a hidden page is not refreshed — it is flagged dirty (options-ui-§11)
  PASS  settings: every Set re-syncs the widgets once; there is no skipRefresh
  PASS  settings: RestoreAllDefaults refreshes once, plus once per session-only row
  PASS  settings: EnsureResetPopup is idempotent
  PASS  settings: the reset dialog is a blocking, escapable confirmation
  PASS  settings: accepting the reset dialog acknowledges in chat
  PASS  settings: the page's tabs are the designed ones, in order, at the designed size
  PASS  settings: no tab holds fewer than two controls
  PASS  settings: every row's group is one of the designed tabs
  PASS  settings: the popup size defaults are the literals they replaced
  PASS  settings: the size sliders cannot travel outside the frame's own clamp
  PASS  settings: the Master controls block is the FIRST group, in canonical order
  PASS  settings: the Master controls rows are the COMPOSER's, not hand-written
  PASS  settings: Enable names the addon, and visibility is a four-value dropdown
  PASS  settings: Enable names the FOLDER this addon loaded from, not a hand-typed copy of it
  PASS  settings: the master rows keep this addon's own shipped defaults
  PASS  settings: the debug console is a SESSION-ONLY row that never reaches db.profile (WG-12)
  PASS  settings: a global reset closes the console a profile reset cannot reach (options-ui-§12)
  PASS  settings: every row on every page carries a `group`
  PASS  settings: every color row is followed by its class-color companion, and none is disabled
  PASS  slash: COMMANDS has a standalone version verb (WG-29)
  PASS  slash: /wg version prints [WG] v<version> on its own line (WG-29)
  PASS  slash: help header has no trailing colon (WG-19)
  PASS  slash: a bare /wg opens the settings landing page through `config`
  PASS  slash: whitespace-only input is treated as bare /wg
  PASS  slash: nil input is tolerated
  PASS  slash: help lists one row per COMMANDS entry, plus the header
  PASS  slash: every COMMANDS row carries a verb, a description and a handler
  PASS  slash: an unknown verb says so and then prints the help index
  PASS  slash: the verb is case-insensitive
  PASS  slash: only the verb is lower-cased — the argument keeps its case
  PASS  slash: /wg version reads the version from TOC metadata
  PASS  slash: /wg version falls back to the in-code constant
  PASS  slash: /wg list prints every schema row
  PASS  slash: /wg list groups rows under their section header
  PASS  slash: /wg list shows current values, not defaults
  PASS  slash: /wg get prints key = value
  PASS  slash: /wg get with no path prints usage
  PASS  slash: /wg get reports an unknown path rather than printing nil
  PASS  slash: /wg get formats a number through the schema fmt
  PASS  slash: /wg set with no path prints usage
  PASS  slash: /wg set reports an unknown path
  PASS  slash: /wg set accepts 'true' as true
  PASS  slash: /wg set accepts '1' as true
  PASS  slash: /wg set accepts 'on' as true
  PASS  slash: /wg set accepts 'yes' as true
  PASS  slash: /wg set accepts 'false' as false
  PASS  slash: /wg set accepts '0' as false
  PASS  slash: /wg set accepts 'off' as false
  PASS  slash: /wg set accepts 'no' as false
  PASS  slash: /wg set bool words are case-insensitive
  PASS  slash: /wg set toggle flips the current value
  PASS  slash: /wg set rejects a non-boolean word and lists the accepted ones
  PASS  slash: /wg set writes a number
  PASS  slash: /wg set rejects a non-numeric value for a number row
  PASS  slash: /wg set clamps a number below the schema min
  PASS  slash: /wg set clamps a number above the schema max
  PASS  slash: /wg set echoes the STORED value back, not the typed one
  PASS  slash: /wg set with a missing value is rejected, not silently applied
  PASS  slash: /wg set enabled false runs the master-switch onChange
  PASS  slash: /wg debug with a bad subcommand prints all three usage lines
  PASS  slash: /wg debug diag is an unknown word: usage, and no report (debug-logging-§14)
  PASS  slash: /wg debug (bare) toggles the console window's visibility
  PASS  slash: /wg debug on does not open the window
  PASS  slash: /wg set writes an enum value from the row's own value set
  PASS  slash: /wg set rejects a value the enum does not offer
  PASS  slash: /wg list carries the Master controls rows under their section
  PASS  slash: /wg enable and /wg disable write the checkbox's own stored path
  PASS  slash: the verbs and the checkbox are the same row, so they cannot disagree
  PASS  slash: `disable` runs the row's onChange, exactly as the checkbox does
  PASS  slash: each verb acknowledges on one `key = value` line
  PASS  slash: the dispatcher answers while the addon is disabled
  PASS  slash: the reserved pair is in COMMANDS, so help and the landing page carry it
  PASS  slash: `/wg show` refuses while disabled, and does not show the popup
  PASS  slash: `/wg test on` refuses while disabled, and does not enter test mode
  PASS  slash: every verb is either on the live list or refuses — there is no third kind
  PASS  slash: the gate lifts the moment the addon is enabled again
  PASS  slash: the refusal REPLACES the handler — not even its own empty-state hint prints
  PASS  slash: the refusal does not touch the help index or the landing page
  PASS  slash: `perf` is reserved here but not registered (LIBKA0S-15)
  PASS  labels: GetGroupTypeLabel Mythic+
  PASS  labels: GetGroupTypeLabel Dungeon by categoryID
  PASS  labels: GetGroupTypeLabel Raid by player count
  PASS  labels: GetGroupTypeLabel fallback Group
  PASS  labels: GetPlaystyleLabel prefers playstyleString
  PASS  labels: GetPlaystyleLabel enum lookup when string empty
  PASS  teleport: GetTeleportSpell picks the known spell from a list
  PASS  teleport: GetTeleportSpell returns first + false when none known
  PASS  teleport: GetTeleportSpell nil when no mapping
  PASS  labels: GetGroupTypeLabel Raid (Current)
  PASS  labels: GetGroupTypeLabel Heroic Raid
  PASS  labels: GetGroupTypeLabel PvP by categoryID 2
  PASS  labels: GetGroupTypeLabel Dungeon by a small player count
  PASS  labels: GetGroupTypeLabel treats exactly 10 players as a Raid
  PASS  labels: GetGroupTypeLabel treats 9 players as a Dungeon
  PASS  labels: GetGroupTypeLabel falls back to Group at zero players
  PASS  labels: Mythic+ outranks every other signal
  PASS  labels: the raid flags outrank categoryID
  PASS  labels: categoryID outranks the player count
  PASS  labels: every playstyle enum maps to its Blizzard-localized wording
  PASS  labels: playstyle None (0) has no label
  PASS  labels: an unmapped playstyle enum yields an empty label, not nil
  PASS  labels: playstyleString wins even when the enum is also set
  PASS  labels: a nil playstyleString falls through to the enum
  PASS  teleport: a scalar mapping returns (spellID, known) directly
  PASS  teleport: a scalar mapping the player has not learned reports known=false
  PASS  teleport: mapID takes precedence over activityID
  PASS  teleport: activityID is the fallback when the mapID is unmapped
  PASS  teleport: a nil mapID and nil activityID resolve to nothing
  PASS  teleport: the FIRST known spell in a candidate list wins
  PASS  teleport: the shipped mapping table is keyed by numbers only
  PASS  teleport: the Midnight Keystone Hero rows match the spellbook-verified IDs
  PASS  teleport: the Midnight season 2 rows match the spellbook-verified IDs
  PASS  teleport: Siege of Boralus offers the spellbook-verified spell first
  PASS  capture: inviteaccepted prefers FRESH when both have mapID
  PASS  capture: inviteaccepted falls back to QUEUED when fresh lacks mapID
  PASS  capture: enabled queues so pendingInfo survives a nil fresh fetch
  PASS  capture: master switch off means nothing is queued
  PASS  capture: master switch off blocks the inviteaccepted fresh fetch too
  PASS  capture: CaptureGroupInfo maps the search-result fields
  PASS  capture: CaptureGroupInfo maps the activity fields
  PASS  capture: CaptureGroupInfo returns nil when the search result is gone
  PASS  capture: missing search-result fields fall back to safe defaults
  PASS  capture: an unknown activity leaves the activity fields at their defaults
  PASS  capture: no activityIDs at all leaves activityID nil
  PASS  capture: fullName falls back to the activity name
  PASS  capture: the legacy `playstyle` field is used when generalPlaystyle is absent
  PASS  capture: the raid flags are carried through
  PASS  capture: applications are matched to captures in FIFO order
  PASS  capture: an 'invited' status changes nothing — it waits for the accept
  PASS  capture: an unrecognized status is ignored
  PASS  capture: accepting clears the queue so a stale apply can't resurface
  PASS  capture: 'applied' with nothing queued is a harmless no-op
  PASS  capture: accepting with no data anywhere leaves pendingInfo nil
  PASS  capture: re-enabling the master switch resumes capturing
  PASS  capture: inviteaccepted resolves the searchResultID via GetApplicationInfo
  PASS  capture: GetApplicationInfo may return a table; the id is read off it
  PASS  capture: an unmapped application falls back to treating appID as the id
  PASS  capture: a missing GetApplicationInfo degrades to the appID path
  PASS  capture: a raising GetApplicationInfo is caught and falls back
  PASS  capture: two outstanding applications pair to their own search results
  PASS  capture: a declined application drops its queued capture
  PASS  capture: a canceled application drops its unanswered capture
  PASS  capture: a timedout application drops its capture
  PASS  capture: an invitedeclined application drops its capture
  PASS  capture: a failed application drops its capture
  PASS  capture: a search field holding false takes the default, not the false
  PASS  capture: an activity field holding false takes the default, not the false
  PASS  capture: a stored zero survives the defaults, because 0 is truthy in Lua
  PASS  notify: no pendingInfo schedules no timer
  PASS  notify: out of a group schedules no timer even with pendingInfo
  PASS  notify: in a group with pendingInfo schedules exactly one timer
  PASS  notify: the scheduled delay comes from notify.delay
  PASS  notify: firing the timer prints the summary and clears the handle
  PASS  notify: a second call for the SAME pendingInfo schedules nothing more
  PASS  notify: both event paths together fire the summary exactly once
  PASS  notify: a NEW pendingInfo is eligible to fire again
  PASS  notify: a re-fire cancels the in-flight timer so two can't race
  PASS  notify: a callback whose pendingInfo was replaced mid-flight prints nothing
  PASS  notify: WipeCapture cancels an in-flight notify so it never fires
  PASS  notify: WipeCapture clears pendingInfo
  PASS  notify: WipeCapture re-arms a later capture (notifiedFor is cleared)
  PASS  notify: the master-switch off-flip wipes an in-flight capture (Schema onChange)
  PASS  notify: autoShow on opens the popup when the timer fires
  PASS  notify: autoShow off prints the summary but never builds the popup
  PASS  notify: autoShow is read when the timer FIRES, not when it is scheduled
  PASS  notify: notify.enabled off prints nothing at all
  PASS  notify: no pendingInfo prints nothing
  PASS  notify: the default summary carries every row
  PASS  notify: the Group row always prints, even with every toggle off
  PASS  notify: notify.showInstance off drops the Instance: row
  PASS  notify: notify.showInstance on keeps the Instance: row
  PASS  notify: notify.showType off drops the Type: row
  PASS  notify: notify.showType on keeps the Type: row
  PASS  notify: notify.showLeader off drops the Leader: row
  PASS  notify: notify.showLeader on keeps the Leader: row
  PASS  notify: notify.showPlaystyle off drops the Playstyle: row
  PASS  notify: notify.showPlaystyle on keeps the Playstyle: row
  PASS  notify: notify.showTeleport off drops the Teleport: row
  PASS  notify: notify.showTeleport on keeps the Teleport: row
  PASS  notify: notify.showClickLink off drops the [Click here to view details] row
  PASS  notify: notify.showClickLink on keeps the [Click here to view details] row
  PASS  notify: the Instance row falls back to Unknown when fullName is empty
  PASS  notify: the Type row prefers shortName over the derived label
  PASS  notify: the Type row derives the label when shortName is empty
  PASS  notify: the Playstyle row is skipped when the label resolves empty
  PASS  notify: the Teleport row is skipped when the map has no teleport spell
  PASS  notify: an unlearned teleport is tagged '(not learned)'
  PASS  notify: a learned teleport carries no '(not learned)' tag
  PASS  notify: a teleport on cooldown is tagged '(on cooldown)'
  PASS  notify: the cooldown tag carries no countdown to go stale
  PASS  notify: an unlearned teleport outranks a cooldown in chat too
  PASS  notify: a ready teleport carries neither tag
  PASS  notify: every summary line carries the shared [WG] prefix
  PASS  notify: a secret-like title degrades in place instead of raising
  PASS  notify: the Leader row still prints when leaderName is nil
  PASS  notify: Playstyle and Teleport drop their rows while Leader keeps its own
  PASS  frame: nothing is created at addon load
  PASS  frame: the first ShowFrame builds and shows the popup
  PASS  frame: buildFrame is one-shot — a second show reuses the same frame
  PASS  frame: ESC-to-close registers the proxy lazily, once, and never the popup itself
  PASS  frame: nothing reaches UISpecialFrames at load, not even after OnEnable (Logout taint)
  PASS  frame: the Close button hides the popup
  PASS  frame: fields render the pending capture
  PASS  frame: with no pendingInfo every field reads 'No data'
  PASS  frame: the no-data playstyle renders the dim em-dash placeholder
  PASS  frame: an empty fullName falls back to Unknown
  PASS  frame: the Type field prefers shortName
  PASS  frame: the Type field derives a label when shortName is empty
  PASS  frame: the Playstyle field prefers the server-rendered string
  PASS  frame: the Playstyle field falls back to the enum label
  PASS  frame: playstyle None (0) renders the dim em-dash, not an empty row
  PASS  frame: re-showing with a new capture re-renders the fields
  PASS  frame: the teleport button registers both click edges, because the up edge is the caster
  PASS  frame: a known teleport wires the secure /cast macro
  PASS  frame: a known teleport renders at full alpha, undesaturated
  PASS  frame: an unlearned teleport shows desaturated at half alpha and casts nothing
  PASS  frame: a teleport on cooldown arms no macro, so the click casts nothing
  PASS  frame: a teleport on cooldown renders desaturated and dimmed
  PASS  frame: a teleport on cooldown says how long is left
  PASS  frame: a teleport on cooldown arms the swipe with the real start and duration
  PASS  frame: a ready teleport clears the note and the swipe
  PASS  frame: an open popup ticks the cooldown note down each second
  PASS  frame: closing the popup cancels the ticker
  PASS  frame: re-opening the popup arms exactly one ticker, not a second
  PASS  frame: a ready teleport arms no ticker at all
  PASS  frame: a popup the gate keeps off screen arms no ticker
  PASS  frame: a popup that reaches the screen later still gets its ticker
  PASS  frame: the ticker rearms the cast the moment the cooldown expires
  PASS  frame: an unlearned teleport says so beside the button
  PASS  frame: an unlearned teleport is never labeled as on cooldown
  PASS  frame: a map with no teleport hides the button entirely
  PASS  frame: the button clears a stale macro when re-shown for a teleport-less map
  PASS  frame: the teleport icon uses the spell's texture
  PASS  frame: no pendingInfo hides the teleport button
  PASS  frame: a first show in combat defers the build and says so
  PASS  frame: leaving combat builds the deferred popup
  PASS  frame: the deferred show restores a pendingInfo cleared during the wait
  PASS  frame: repeated in-combat shows queue exactly one deferred show
  PASS  frame: a show requested in combat is deferred, not forced
  PASS  frame: a popup held at alpha 0 comes back in combat without a Show
  PASS  frame: reconfiguring the teleport button in combat stashes and replays it
  PASS  frame: a fresh profile leaves the popup at its default center anchor
  PASS  frame: dragging the title bar persists the popup position
  PASS  frame: a saved position is restored on the next build
  PASS  frame: the popup is built at the profile's width and height
  PASS  frame: a stored size is honored on build
  PASS  frame: a size change re-sizes a popup that is already open
  PASS  frame: a size hand-edited past the clamp is drawn at the nearest legal value
  PASS  frame: a non-numeric stored size falls back to the shipped default
  PASS  frame: a size change taken in combat is refused, and lands on the next open
  PASS  frame: the popup opens at the profile's master scale
  PASS  frame: a scale change re-scales a popup that is already open
  PASS  frame: a scale hand-edited past the clamp is drawn at the nearest legal value
  PASS  frame: a scale change taken in combat is refused, and lands on the next open
  PASS  frame: the popup opens at the profile's master alpha
  PASS  frame: an alpha change lands DURING combat, unlike a size or scale change
  PASS  frame: locking the popup stops the title bar starting a drag
  PASS  frame: Reset position re-anchors the popup and forgets the saved point
  PASS  frame: Reset position in combat is refused, but still forgets the saved point
  PASS  frame: visibility 'never' refuses every path to the screen
  PASS  frame: visibility 'inCombat' BUILDS out of combat but only SHOWS in it
  PASS  frame: visibility 'outOfCombat' is the mirror of it
  PASS  frame: an unrecognized visibility value fails OPEN, not closed
  PASS  frame: switching visibility to 'never' hides a popup that is already open
  PASS  frame: both combat-transition events are registered, and to one handler
  PASS  frame: entering combat does NOT attempt a hide the client would refuse
  PASS  frame: 'never' set during combat is honored the moment the lockdown lifts
  PASS  frame: Close pressed in combat is remembered, not fired into a refusal
  PASS  frame: a deferred Close outranks a gate that would still permit the popup
  PASS  frame: a combat edge brings back a popup the gate had hidden
  PASS  frame: Close in combat takes the popup off screen at once
  PASS  frame: the real Hide lands when the lockdown lifts, and the alpha comes back with it
  PASS  frame: the alpha restored is the player's own, not a hardcoded 1
  PASS  frame: 'out of combat' takes the popup off screen the moment combat starts
  PASS  frame: and it opens again by itself when combat ends
  PASS  frame: a popup the PLAYER closed does not come back when combat starts
  PASS  frame: a popup closed with ESC does not come back either
  PASS  frame: Escape in combat never calls the popup's protected Hide, and soft-hides it
  PASS  frame: Escape out of combat really hides the popup, and the next Escape opens the menu
  PASS  frame: Escape in combat is a player dismissal -- regen lands the real Hide, and nothing reopens it
  PASS  frame: Escape in combat clears a gate flag left over from a re-show
  PASS  frame: the proxy is shown exactly while the popup is on screen
  PASS  frame: leaving combat does not reopen a popup the player closed mid-fight either
  PASS  frame: a combat transition never opens a popup with nothing to show
  PASS  frame: PLAYER_REGEN_DISABLED is answered from the event, not from a lockdown flag that has not flipped
  PASS  frame: a combat transition with no popup built is a no-op, not an error
  PASS  frame: reopening a soft-hidden popup in combat with no capture never Hides the secure button
  PASS  frame: a deferred no-capture configure is replayed, not dropped
  PASS  frame: a gate-declined reopen in combat leaves a soft-hidden popup at alpha 0, and the launcher still closes it
  PASS  frame: an alpha write while soft-hidden does not reveal the popup
  PASS  frame: a real show after the soft hide restores the master alpha
  PASS  frame: a stand-down in combat drops a queued first show and a queued teleport configure
  PASS  frame: reconfiguring the teleport button reuses the same three script handlers
  PASS  panel: OnEnable registers the parent category and the General subcategory
  PASS  panel: the parent category is added to the AddOns list
  PASS  panel: Register is idempotent — a second call registers nothing more
  PASS  panel: a registration taken in combat is parked and lands at PLAYER_REGEN_ENABLED, with no second host call
  PASS  panel: a login taken in combat is parked and lands at combat end, with no second registration
  PASS  panel: registration validates the schema
  PASS  panel: both panels start hidden
  PASS  panel: registration creates no AceGUI widgets
  PASS  panel: OnShow itself builds nothing; the deferred hop does
  PASS  panel: the build is one-shot across repeated shows
  PASS  panel: two OnShows before the hop runs still build only once
  PASS  panel: the Defaults button is built lazily, on the General page only
  PASS  panel: clicking Defaults raises the confirmation popup rather than resetting
  PASS  panel: confirming the popup restores defaults
  PASS  panel: the reset dialog is not registered before it is needed (taint)
  PASS  panel: every schema row renders a widget, on its own tab
  PASS  panel: the strip draws one tab per schema group, in declaration order
  PASS  panel: bool rows render checkboxes, number rows sliders, enum rows dropdowns
  PASS  panel: widgets open showing the current profile value
  PASS  panel: the slider inherits its bounds and step from the schema row
  PASS  panel: a tabbed page draws its group names as TABS, never as headings
  PASS  panel: a mixed tab draws its SUBGROUPS as headings (options-ui-§7)
  PASS  panel: no subgroup heading repeats its own tab's name
  PASS  panel: paired rows get half width, solo rows go full width
  PASS  panel: the Chat group renders its Test action button
  PASS  panel: the Master controls tab closes with the reset button PAIR
  PASS  panel: Reset all settings raises the confirmation, it does not reset on the click
  PASS  panel: Reset position drops the stored point and re-anchors
  PASS  panel: the Test button runs the same path as /wg test notify
  PASS  panel: a throwing button onClick is caught, not propagated
  PASS  panel: the Debug console renders as an ordinary Master controls checkbox
  PASS  panel: ticking Debug console shows the window without touching db.profile
  PASS  panel: unticking Debug console hides the window
  PASS  panel: opening the console while General is OPEN moves the checkbox
  PASS  panel: re-opening General re-syncs the Debug console checkbox
  PASS  panel: ticking a checkbox writes through to db.profile
  PASS  panel: a checkbox coerces its value to a real boolean
  PASS  panel: releasing the slider writes through to db.profile
  PASS  panel: the slider snaps its committed value to the schema step
  PASS  panel: unticking Enable fires the master-switch onChange
  PASS  panel: rendering registers one refresher per rendered widget
  PASS  panel: a re-render REPLACES the refresher list rather than growing it
  PASS  panel: a /wg set re-syncs the open widget
  PASS  panel: RestoreAllDefaults re-syncs every open widget once
  PASS  panel: a throwing refresher does not abort the remaining ones
  PASS  panel: the scroll container is patched to always show its scrollbar
  PASS  panel: the patch is one-shot per widget
  PASS  panel: releasing the widget restores AceGUI's stock behavior
  PASS  panel: the landing page lists one row per slash command
  PASS  panel: the landing page shows the TOC Notes line
  PASS  panel: the landing page renders the Slash Commands heading and the logo
  PASS  panel: the landing logo path is built from the folder this copy loaded from
  PASS  panel: the landing page adds logo, notes, heading and command rows in that order
  PASS  panel: a dirty landing page re-renders in place instead of stacking a second copy
  PASS  testmode: bare /wg test toggles test mode, and the checkbox follows
  PASS  testmode: /wg test on|off sets it, and repeating either changes nothing
  PASS  testmode: /wg test in combat is refused with one line and leaves it off
  PASS  testmode: /wg test with an unknown word prints usage and changes nothing
  PASS  testmode: the COMMANDS row describes the mode and the notify sub-word
  PASS  testmode: the Test mode row is composed right after the Debug console, on its own line
  PASS  testmode: the row's tooltip is this addon's, not the composer's generic one
  PASS  testmode: it is session-only and never reaches db.profile
  PASS  testmode: ticking the box shows the popup with placeholder content, pendingInfo untouched
  PASS  testmode: with no capture at all, it still shows the placeholder and leaves pendingInfo nil
  PASS  testmode: it shows the popup whatever General visibility and Open Automatically say
  PASS  testmode: unticking hides the popup and puts the real capture back
  PASS  testmode: the popup's Close button ends test mode and unticks the box
  PASS  testmode: ESC ends test mode and unticks the box
  PASS  testmode: the lock is honored while it is up
  PASS  testmode: a start in combat from the checkbox is refused by the library's lock, box unticked
  PASS  testmode: combat starting ends it, says so once, and unticks the box
  PASS  testmode: combat with test mode off prints nothing about it
  PASS  testmode: Reset all settings ends it
  PASS  testmode: /wg test notify is the one-shot notify + popup flow, and ends test mode
  PASS  testmode: /wg show ends it and shows the real capture
  PASS  testmode: the join popup does NOT end it; the capture waits for the chat link
  PASS  testmode: the sample capture is a fresh table each time
  PASS  snapshot: a fresh session reports empty capture tables
  PASS  snapshot: an apply shows under byResult, and applied moves it to applications
  PASS  snapshot: the capture copy is deep, so a report cannot mutate a capture
  PASS  snapshot: notified is true only for the pendingInfo notify fired for
  PASS  snapshot: the frame accessor reports not built, and never builds the popup
  PASS  snapshot: a shown popup reports built, shown, on screen and its live point
  PASS  snapshot: a first show in combat reports the queued replay
  PASS  snapshot: a hide in combat reports soft-hidden with a Hide owed
  PASS  snapshot: the frame copy is fresh, so a report cannot edit the queue
  PASS  diagnostics: the host identity names schema, profile, enabled, stood-down, holds, test mode
  PASS  diagnostics: always-print rows print at default, a changed row prints path = value (default)
  PASS  diagnostics: stood down, the runtime sections say so and the header shows the hold
  PASS  diagnostics: the report reads and never acts: no registration, hold, timer or save
  PASS  diagnostics: registration health names every event and the chat-link route
  PASS  diagnostics: the client's applications print beside the capture tables
  PASS  diagnostics: pending info prints leader, voice chat and title, and the notify timer
  PASS  diagnostics: teleport resolution names the spell, known flag, table entry and cooldown
  PASS  diagnostics: an unreadable cooldown is named, never computed on
  PASS  diagnostics: no pending group reads as such, not as an empty teleport
  PASS  diagnostics: an unbuilt popup reads `not built`, and the report never builds it
  PASS  diagnostics: a built popup prints its state, the saved point and the live point
  PASS  diagnostics: the launcher line
  PASS  diagnostics: a raising section costs exactly one line, and the report still ends
  PASS  diagnostics: an over-cap report ends `truncated`, then the end marker
  PASS  diagnostics: a secret-like pending value degrades in place instead of raising
  PASS  diagnostics: one localized chat line names the count and Copy
  PASS  diagnostics: without LibKa0s-DebugLog both forms print the library-absent line, writing nothing
  PASS  launcher: it registers at login, and the broker object IS the minimap button's
  PASS  launcher: the object is a launcher, named for the FOLDER, wearing this addon's logo
  PASS  launcher: the label is the BRAND NAME in plain text (launcher-§1)
  PASS  launcher: the label is NOT wired to the TOC Title
  PASS  launcher: the name is the FOLDER this copy loaded from, not a hand-typed literal
  PASS  launcher: Register is idempotent -- a second call builds no second button
  PASS  launcher: the icon file exists and is an uncompressed 32-bit TGA
  PASS  launcher: the TOC's IconTexture is the same file the object wears
  PASS  launcher: LEFT-click opens the settings panel, and never the popup
  PASS  launcher: LEFT-click opens the panel while disabled too, with no refusal line
  PASS  launcher: RIGHT-click opens a menu titled with the label, with all four entries in order
  PASS  launcher: the Enabled entry runs the /wg disable|enable body, ack and all
  PASS  launcher: the Locked entry runs `/wg set locked toggle`, the Lock frame row's write
  PASS  launcher: the Test mode entry runs the bare /wg test body
  PASS  launcher: the Show window entry toggles the group popup through WhatGroup:ToggleFrame
  PASS  launcher: a Show window dismissal ends test mode, as the Close button does
  PASS  launcher: while disabled only Enabled is live; the rest are grayed and call nothing
  PASS  launcher: with no MenuUtil the right click falls back to the settings panel
  PASS  launcher: the descriptor passes the four pairs to the verbs' seams, and no retired field
  PASS  launcher: the tooltip reads title, status, lock, test mode, then the two click hints
  PASS  launcher: Locked and Test mode are read on every hover, from the rows' own stores
  PASS  launcher: while disabled the tooltip still draws, with the same two click hints
  PASS  launcher: the tooltip's version is the TOC's, not the in-code constant
  PASS  launcher: there is no host tooltip hook, and the retired label's locale row is gone
  PASS  launcher: the Minimap button row is stored, global, and LibDBIcon's OWN hide key
  PASS  launcher: the row's get/set invert, and the button follows immediately
  PASS  launcher: the row's CLI path reads in its own sense
  PASS  launcher: a legacy store with hide = true reads not-shown, and nothing moves
  PASS  launcher: a button the player hid survives Reset all settings (options-ui-§12)
  PASS  launcher: a button the player hid survives the General page's Defaults button
  PASS  launcher: a button the player hid survives the Master controls reset button
  PASS  launcher: LibDBIcon's own writes into the table are not disturbed
  PASS  launcher: an install with neither broker library loads, and says so once
  PASS  launcher: with no LibDBIcon the broker plugin still registers
  PASS  launcher: the row still stores with no broker library at all
  PASS  launcher: with LibKa0s absent the seam answers honestly and the store still moves
  PASS  lifecycle: the addon exposes no public global (WG-01)
  PASS  lifecycle: NS IS the addon object (AceAddon mixes into the namespace)
  PASS  lifecycle: earlier files' fields survive NewAddon
  PASS  lifecycle: the shared chat prefix is the cyan [WG] tag
  PASS  lifecycle: NS.Print, WhatGroup._print and NS.Util.print are one seam
  PASS  lifecycle: debug state is session-only and starts off
  PASS  lifecycle: OnInitialize builds the db from the schema defaults
  PASS  lifecycle: OnInitialize registers both slash verbs
  PASS  lifecycle: OnEnable registers the two capture events
  PASS  lifecycle: no events are registered before OnEnable
  PASS  lifecycle: OnEnable seeds wasInGroup from the current roster state
  PASS  lifecycle: the ApplyToGroup hook installs at file load
  PASS  lifecycle: the ApplyToGroup hook routes into the capture pipeline
  PASS  chat link: the details link is Blizzard's addon link type, addon:WhatGroup:show
  PASS  chat link: a click through SetItemRef opens the popup and never reaches the ItemRef fallthrough
  PASS  chat link: a shift-click opens the popup and never reaches HandleModifiedItemClick
  PASS  chat link: a stale link prints the hint, opens nothing and never reaches the fallthrough
  PASS  chat link: another addon's addon: link is not ours
  PASS  chat link: an item link goes to the ItemRef tooltip, not to us
  PASS  chat link: the SetItemRef callback registers at file load, exactly once
  PASS  lifecycle: disable drops the SetItemRef callback and enable restores exactly one
  PASS  chat link: degraded (no EventRegistry) falls back to the WhatGroup: link and the post-hook
  PASS  chat link: degraded (no LinkTypes.AddOn) falls back to the WhatGroup: link and the post-hook
  PASS  chat link: the degraded post-hook ignores links that aren't ours
  PASS  chat link: the degraded post-hook ignores a non-string link argument
  PASS  lifecycle: joining a group with a capture waiting fires the notify
  PASS  lifecycle: a roster tick while already grouped is not a transition
  PASS  lifecycle: leaving the group wipes the capture
  PASS  lifecycle: leaving the group cancels an in-flight notify
  PASS  lifecycle: rejoining after a leave fires a fresh notify
  PASS  lifecycle: the retail ordering (ROSTER before inviteaccepted) still notifies
  PASS  lifecycle: InitSummary reflects live runtime state
  PASS  lifecycle: InitSummary is safe before the db exists
  PASS  lifecycle: /wg config opens the parent settings category
  PASS  lifecycle: /wg config is refused during combat (options-ui-§2)
  PASS  lifecycle: a login taken in combat registers the panel at combat end
  PASS  lifecycle: /wg test notify injects a synthetic capture and runs the full flow
  PASS  lifecycle: /wg test notify refuses while the master switch is off
  PASS  lifecycle: the panel Test button previews while the addon is disabled
  PASS  lifecycle: /wg test notify fires immediately, without the notify delay
  PASS  lifecycle: /wg show opens the popup when a capture exists
  PASS  lifecycle: /wg show with no capture prints a hint and opens nothing
  PASS  lifecycle: /wg reset <path> resets one setting, with no confirmation
  PASS  lifecycle: a bare /wg reset explains the change rather than resetting or erroring
  PASS  lifecycle: /wg resetall asks for confirmation rather than resetting outright
  PASS  lifecycle: /wg resetall and the Defaults button share one OnAccept body
  PASS  debuglog: FONT_MONO points at the library payload's JetBrains Mono TTF
  PASS  debuglog: the console renders in the vendored TTF when the client can fetch it
  PASS  debuglog: a TTF the client cannot fetch falls back to a Blizzard font (debug-logging-§2)
  PASS  debuglog: FormatPlain wraps the tag in brackets, single-space separators
  PASS  debuglog: FormatPlain tolerates a nil tag
  PASS  debuglog: FormatColored colors timestamp + tag; pipe and content default
  PASS  debuglog: /wg debug on enables session state
  PASS  debuglog: /wg debug off disables session state
  PASS  debuglog: /wg debug (no arg) toggles the window, not the state
  PASS  debuglog: header toggle click flips debug state
  PASS  debuglog: enabling writes a '[Debug] logging enabled' console line
  PASS  debuglog: enabling debug appends the [Init] session summary after the bracket (debug-logging-§5)
  PASS  debuglog: [Init] fires only on enable, not on disable (debug-logging-§5)
  PASS  debuglog: disabling still appends a '[Debug] logging disabled' line
  PASS  debuglog: NS.Debug survives an unsafe format arg without raising (WG-22)
  PASS  debuglog: NS.Debug is a no-op (no console write) when debug is off
  PASS  debuglog: debug-logging-§11 scrollbar + line-counter sync is a safe no-op under the mock
  PASS  debuglog: settings change logs one [Set] line at the write seam (debug-logging-§10)
  PASS  debuglog: RestoreAllDefaults logs one [Set] reset profile line counting the rows it changed (debug-logging-§10)
  PASS  debuglog: RestoreAllDefaults on a pristine profile counts 0 rows (debug-logging-§10)
  PASS  debuglog: a profile reset from outside the helper is logged once, without a count (debug-logging-§10)
  PASS  debuglog: the library's page reset is one [Set] line counting the rows it changed (debug-logging-§10)
  PASS  debuglog: an all-default page reset logs 0 rows, not a line per row (debug-logging-§10)
  PASS  debuglog: the bulk bracket adds no line when the act reset the profile (debug-logging-§10)
  PASS  debuglog: a nested bracket logs once, at the outermost close, with the summed tally (debug-logging-§10)
  PASS  debuglog: a nested bracket that reset the profile silences the outer line (debug-logging-§10)
  PASS  debuglog: a bracket that closes on an error still logs its tally and unmutes (debug-logging-§10)
  PASS  debuglog: a write that raises inside a bracket is not counted (debug-logging-§10)
  PASS  debuglog: a profile reset that raises logs one marked line and re-raises (debug-logging-§10)
  PASS  debuglog: a profile reset that raises leaves no count for a later reset (debug-logging-§10)
  PASS  debuglog: a profile reset inside an open bracket silences the bracket (debug-logging-§10)
  PASS  debuglog: a profile copy logs one [Set] copied line naming the source (debug-logging-§10)
  PASS  debuglog: InitSummary leads with the debug-logging-§5 identity fields, then runtime state
  PASS  debuglog: enable ack is color-coded green/red matching the header (debug-logging-§5)
  PASS  debuglog: pin — a vanished search result logs the [Capture] nil line
  PASS  debuglog: pin — an apply logs the [Apply] captured line
  PASS  debuglog: pin — every application status logs the [LFG] appID/status line
  PASS  debuglog: pin — an accepted invite with a capture logs the [Invite] line naming it
  PASS  debuglog: pin — an accepted invite with no capture logs the [Invite] no-capture line
  PASS  debuglog: pin — a roster transition logs the [Roster] line
  PASS  debuglog: pin — the details link logs the [ChatLink] click line
  PASS  debuglog: pin — an accepted invite with nothing pending logs the [Notify] skip line
  PASS  debuglog: pin — a scheduled join notify logs the [Notify] scheduling line
  PASS  debuglog: pin — a wipe with a reason and something in flight logs the [Capture] wiped line
  PASS  debuglog: pin — /wg test notify logs the [Test] injection line
  PASS  debuglog: pin — showing a capture logs the [Frame] popup-shown and teleport lines
  PASS  debuglog: pin — showing with no capture logs the [Frame] fallback and nil teleport lines
  PASS  debuglog: pin — a show the visibility gate withholds logs the [Frame] not-shown line
  PASS  debuglog: pin — unticking test mode logs the [Test] off line with its reason
  PASS  docmap: every Tier 2 row agrees with what docs/ holds
  PASS  lintconfig: .luacheckrc sets no top-level ignore
  PASS  lintconfig: .luacheckrc switches no warning class off wholesale
  PASS  lintconfig: every files[...] ignore is narrowed to a file or a name
  PASS  lint: exclude_files carries the template's frozen stores
  PASS  lint: read_globals grants no removed or unread global
  PASS  lintconfig: no source file carries a bare inline luacheck ignore
  PASS  docs/ARCHITECTURE.md carries the ten sections documentation-§3 names
  PASS  every mandated hub section that has a topic doc has spilled into it
  PASS  every anchor pointing into docs/ARCHITECTURE.md resolves to a heading
  PASS  the player-facing history has the ONE home documentation-§1 allows, and no second
  PASS  README.md's top-level sections are the ones documentation-§1 names, in its order
  PASS  README.md's Reporting a bug section is the standard's text with /wg, and links nowhere
  PASS  the README's settings table is page-granular, not per-tab
  PASS  every settings tab the README sends a player to exists in the schema
  PASS  docs/smoke-tests.md carries a non-English-client section
  PASS  every evidence id the register cites is assigned by its bundle in docs/audits/ or docs/reviews/
  PASS  disabled 1: enabled, the addon holds a NON-EMPTY registration set
  PASS  disabled 3: the registration set is EMPTY, by count and by name
  PASS  disabled 3: the write seam is the route — the checkbox and the verb reach the same latch
  PASS  disabled: no raw frame registration exists at any point, in combat or out
  PASS  disabled 4: no timer, ticker or OnUpdate survives, and none is armed afterwards
  PASS  disabled 5: every frame shown while enabled is hidden, and the show ladder answers no
  PASS  disabled 6: firing every event it used to watch writes nothing, says nothing, shows nothing
  PASS  disabled 7: every reserved verb answers normally, and the bare /wg opens the panel
  PASS  disabled 7: each FEATURE verb answers exactly one refusal line and reaches no write seam
  PASS  disabled 8: left-click opens the panel; right-click's menu grays every feature entry
  PASS  disabled 9: re-enabling restores the registration set exactly
  PASS  disabled 9: a setting changed WHILE DISABLED is what the rebuild reflects
  PASS  disabled 10: releasing the perf hold does NOT resurrect an addon `disabled` still holds down
  PASS  disabled 10: the other order — disabled first, perf released last
  PASS  disabled 10: the latch persists nothing
  PASS  disabled: the chat command, the panel, the db callbacks and the launcher all survive
  PASS  disabled: a profile switch that flips `enabled` is re-evaluated, both ways
  PASS  disabled: a stand-down in combat holds the protected Hide pending, and one event with it
  PASS  libs/LibKa0s is the LibKa0s release CLAUDE.md says this addon bundles
  PASS  tests/_kit is the test kit that shipped with that release
  PASS  the automated-test runner is recorded executable (100755)
  PASS  eol: every tracked file carries the terminator .gitattributes declares for it
  PASS  eol: .gitattributes is line-endings-§5's canonical body for this repo kind
  PASS  prose: no authored file carries a British spelling from localization-§5's published list
  PASS  prose: the gate carries localization-§5's two lists whole, and nothing of its own
  PASS  prose self-test: the carve-out suppresses the named generated folder, and only it
  PASS  prose self-test: a path the carve-out does not name is not covered by one that looks like it
  PASS  prose self-test: a carve-out that is not a set of path strings is a failure, not a silence
  PASS  prose self-test: a TOC's file lines are read as paths, and its directives and comments are not
  PASS  prose self-test: a .pkgmeta's ignore block is read, and the keys around it are not
  PASS  prose self-test: an ignore entry covers a path exactly, by folder, and by wildcard
  PASS  prose self-test: the carve-out admits a generated dump and refuses a file the TOC loads
  PASS  prose self-test: a waiver-file exclusion meets the same two refusals as the carve-out
  PASS  prose self-test: each list is refused on the matching rule its own scan uses
  PASS  prose self-test: the scan and the refusals read the added exclusions through one reader
  PASS  prose self-test: a narrowing is refused by what it suppresses, not by how it is written
  PASS  prose self-test: the disclosure names what each entry suppressed, and says when it is bounded
  PASS  prose self-test: a malformed waived is a failure, not a silence
  PASS  layoutcap: every authored file over the 1500-line cap is named in the census
  PASS  layoutcap: no census row outlives the breach it records
  PASS  layoutcap: every over-cap census row carries one of layout-§1's three terminal states
  PASS  layoutcap: the census and the exempt set agree about which paths were exempted
  PASS  layoutcap: an empty census is written as a result rather than left standing empty
  PASS  layoutcap self-test: the parser reads the census nested under the register, and stops there
  PASS  layoutcap self-test: a census outside its register, or at the wrong level, is not read
  PASS  layoutcap self-test: an over-cap file missing from the census is reported, and an exempt one is not
  PASS  layoutcap self-test: a census row that outlives its breach is reported
  PASS  layoutcap self-test: an over-cap row that names no terminal state is reported
  PASS  layoutcap self-test: the census and the exempt set are held to naming the same paths
  PASS  layoutcap self-test: a census that states nothing is told apart from one that states none
  PASS  layoutcap self-test: the exempt set takes folders as well as paths
  PASS  diagnostics contract: both forms run the report
  PASS  diagnostics contract: the debug word is matched in any case
  PASS  diagnostics contract: both markers carry the brand and the end counts the report
  PASS  diagnostics contract: the report appends after what the console already holds
  PASS  diagnostics contract: the report lands with logging off and leaves it off
  PASS  diagnostics contract: both forms run while the addon is disabled
  PASS  diagnostics contract: no other name runs the report

823 passed, 0 failed, 0 skipped, 823 total
