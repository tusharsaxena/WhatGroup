# Summary: LibKa0s v1.55.0 -> v1.56.0

## The tag, and the minors

- **Tag:** v1.55.0 -> v1.56.0, taken from the sibling checkout's local tag with `git archive`. The
  base comes from the `CLAUDE.md` provenance line, and the vendored payload matched v1.55.0 byte for
  byte (`01_DELTA.md` 3a).
- **Kit revision:** 25 -> 26.
- **Per-file minors:** 15 of 21 files move forward. Core 8, Lifecycle 2, Bus 2, Schema 2, Item 2,
  Media 4, Widgets 10, DebugLog 13, Slash 15, Launcher 2, Options 24, OptionsWidgets 31,
  OptionsTabs 4, OptionsScroll 4 and Perf 13. Env, Compat, Pool, WidgetsDragHandle, OptionsCompose
  and PerfPanel do not move. No file is added or removed, and there is no cross-major skew. The full
  table is in `01_DELTA.md` 3c.
- **Re-vendor commit:** the `RV-WG` commit, which carries both payloads, the provenance line in
  `CLAUDE.md` and this bundle.
- **Base correction (Step 0):** none owed. The v1.55.0 bundle's base (v1.54.2) matches the history.
- **Span bundle (3h):** not written here. 28 vendored tags have no bundle, and the listing is in
  `01_DELTA.md` 3h. Plan item WG-29 writes the consolidated span bundle.

## Delivered free (class A)

Reached WhatGroup on the copy alone, with no host change:

- **Core 8:** `printer.Format` survives a secret in a numeric slot.
- **Slash 15:** `/wg set` prints the write seam's refusal instead of echoing the old value.
- **Launcher 2:** the missing-library notices print once, without the `[LibKa0s] ` prefix.
- **Media 4:** JetBrains Mono is flagged western + ruRU, so it reaches the font dropdowns on the
  ruRU client.
- **DebugLog 13:** the buffer trim is batched.
- **Options 24:** a registration requested in combat parks and replays when combat ends.
  `OpenOptionsPanel` answers a boolean.
- **OptionsTabs 4 and OptionsWidgets 31:** the page chrome no longer leaks a widget per render, and
  the drag throttles keep their own armed flag.
- **Kit 26:** `asserts.lua`, `prose_lists.lua` and `mock_events.lua` split out.
  `Kit.assertErrorMatches` and `Kit.assertLibraryConstant` arrive. The kit adds a recording
  `EventRegistry` and `C_EventUtils`, starts frames shown and makes the AceDB fake raise. It also
  adds the lone-CR eol gate and store-root prose scanning.

## Contract blockers (3g)

- **Options minor 24's combat park.** Documents: `Options/version-23.30.3.7.3-docs.md` ->
  `Options/version-24.31.4.7.4-docs.md:39-65`.
- **What goes red:** three host cases pin "registers at once in combat". They are
  `tests/test_panel.lua:106`, `tests/test_panel.lua:114` and `tests/test_lifecycle.lua:413`.
- **Resolution:** deferred by plan to **WG-02**. The `RV-*` commits are copy-only, and the three
  reds are listed in the commit body.

## Adopted

Nothing in this run. The opt-ins that v1.56.0 offers WhatGroup are M3 plan items:

- Core's `SafeRegisterEvent` family: **WG-06**.
- The Schema adoption, with `SetMany` and `writeThrough`: **WG-12**.
- Launcher's `isEnabled` / `disabledLine`, with `Kit.assertLibraryConstant` on the Slash stub:
  **WG-13**.
- The kit's recording `EventRegistry` in place of the local fake: **WG-10**.

## Declined

None in this run. Bus remains declined (#21, `state:will-not-do`).

## Skipped or unreached

Steps 5 to 7 (candidates, interview, implementation) are not run. They are owned by the plan's M3
items above.

## Gates

Tests come from `ka0s-bounded lua5.1 tests/run.lua`, lint from `ka0s-bounded luacheck .`, and
complexity from `ka0s-bounded lizard -l lua -x "./libs/*" -x "./tests/_kit/*" .`.

| Point | Tests | Lint | Lizard |
|---|---|---|---|
| Before the copy (v1.55.0) | 728 passed, 0 failed, 0 skipped | 0 / 0 in 48 files | not run |
| After the copy (v1.56.0) | 725 passed, 3 failed, 0 skipped, 728 total | 0 / 0 in 48 files | 0 functions above CCN 15 |

The three failures are the Options-park cases named under *Contract blockers*. No tool was
skipped. `docs/test-cases.md` and the README test badge are not regenerated by this commit;
WG-DOCS does that.
