# Decisions (WhatGroup)

- **C1 `O.NavRail`:** Not adopted here. No WhatGroup page edits one instance out of many through sub-pages retargeted by one picker. tests/loader.lua's hand-typed LIBKA0S list gains OptionsNav.lua in XML order, and the stub answers the NavRail the loaded file publishes.
- The library-absent stub gains `NavRail` (or, where the host keeps a hand-typed library list, that list gains `OptionsNav`), so the surface-parity case stays green.
- Plan: `Ka0sAddonsCommonTasks/docs/2026-09-26-NAVRAIL_ADOPTION/`.
